apiVersion: apps/v1
kind: Deployment
metadata:
  name: mi-despliegue
  namespace: ronybane-dev
  labels:
    app: mi-despliegue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mi-despliegue
  template:
    metadata:
      labels:
        app: mi-despliegue
    spec:
      containers:
        - name: api-notificaciones-whatsapp
          image: 'quay.io/ronybane/api-notificaciones-whatsapp:latest'
          envFrom:
            - secretRef:
                name: quaycredenciales
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
      imagePullSecrets:
        - name: quaycredenciales  # Aquí especificas tu Secret para las credenciales del registry
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
